# 
# Alejandro's bashrc
#

# vi mode
set -o vi

# 
# BASH customizations
#
export PATH=$PATH:/usr/local/bin:/opt/bin
export DOTFILES=${HOME}/Code/github.com/asajaroff/dotfiles

# History
HISTFILE="${HOME}/.bash_history"
export HISTCONTROL=ignoreboth,erasedups
export HISTTIMEFORMAT="%h %d %H:%M:%S "
export HISTIGNORE="ls:ps:history"
export HISTSIZE=10000000
export SAVEHIST=${HISTSIZE}
shopt -s cmdhist

#
# Aliases
#
source ${DOTFILES}/config/aliases

# Functions
source ${DOTFILES}/private/clone.sh
source ${DOTFILES}/private/clone.sh

# EEN 
EEN_FUNCTIONS_DIR="${HOME}/.dotfiles/private/een/functions/"
EEN_FUNCTIONS=$(ls -1 ${EEN_FUNCTIONS_DIR})
echo $EEN_FUNCTIONS

if [[ -d ${EEN_FUNCTIONS_DIR} ]]; then
	source ${DOTFILES}/private/een/functions/java-utils.sh
	source ${DOTFILES}/private/een/functions/kubernetes.sh
	source ${DOTFILES}/private/een/functions/on-prem.sh
	source ${DOTFILES}/private/een/functions/vpn.sh
fi

# Preferred editor for local and remote sessions
if [[ -n $SSH_CONNECTION ]]; then
  export EDITOR='vim'
else
  export EDITOR='nvim'
fi

# Krew
#
if [[ -d ${HOME}/.krew ]]; then
  export PATH="${KREW_ROOT:-$HOME/.krew}/bin:$PATH"
fi


# Starship shell
eval "$(starship init bash)"

# Bash completions
. <(asdf completion bash)
. <(tldr --print-completion bash)

# Coloured man pages
export MANPAGER="less -R --use-color -Dd+r -Du+b"
export MANROFFOPT="-P -c"
